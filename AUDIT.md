# 缺陷审计与修复状态

日期：2026-09-06。独立探索测试确认的四个缺陷，以及首次 Linux CI 暴露的低帧率计时缺陷均已修复，并转为持续回归测试。

| 缺陷 | 修复 | 回归证据 |
| --- | --- | --- |
| 手势收到 `touchcancel` 仍会发射 | 取消事件只清除当前指针，不创建弹珠 | `AUDIT-01` 通过，并验证取消后的下一次正常触摸仍可发射 |
| 关闭说明后空格重新打开说明 | 继续游戏时将键盘焦点交还弹盘 | `AUDIT-02` 通过 |
| 820×600 横屏无法看到完整弹盘 | 短横屏使用按可用高度计算的三栏紧凑布局 | `AUDIT-03` 及 844×390、568×320 通过 |
| 鼠标右键误发射 | 鼠标瞄准只接受主键 | `AUDIT-04` 通过 |
| 低帧率下 16 秒回收与 650ms 结算被拉长 | 回收、轻推和结算使用 Phaser 未平滑的活跃墙钟；Matter 仍使用平滑步长 | 保留自然发射闭环，并覆盖命中、分裂、轻推、回收和结算路径 |

首次远端 CI 的失败 trace 显示：真实经过约 22.4 秒时，弹珠年龄只从 33ms 增长到约 1.98 秒。原因是 Phaser 为物理稳定而平滑的 Scene delta 被误用于产品承诺的实际时间。修复后，仅回收、停滞检测和待结算等待使用 `game.loop.rawDelta`；暂停期间 Scene 不更新，浏览器从后台恢复时 Phaser 会重置该值，所以暂停时间不会被补算。

另修复了 320×568 下由 `min-width: 340px` 引起的横向溢出。当前测试在 320×568、390×844、568×320、820×600、844×390 等尺寸验证弹盘与操作按钮均位于视口内。

修复前的原始记录、失败测试和截图保留在 [`evaluation/baseline/`](evaluation/baseline/)。修复后的测试报告与录屏清单见 [`evaluation/current/`](evaluation/current/)。

## 当前验证命令

```sh
npm run format:check
npm audit --audit-level=high
npm test
npm run build
npm run test:e2e
```

不能自动确认的范围仍包括真实 iOS/Android 设备、Safari/Firefox 的手工体验、扬声器听音以及真实系统后台切换。这些范围不会被测试报告表述为已验证。
