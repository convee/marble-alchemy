# 独立缺陷验收记录

日期：2026-09-05。本轮按“自行测试、查找缺陷”执行，未修改 `src/` 游戏逻辑。新增独立探索脚本、四个回归测试和证据。测试目标为生产预览 http://127.0.0.1:4173/ 。

结论：确认四个可复现缺陷。构建和原有测试通过，不能覆盖这些缺陷。

## 已确认缺陷

### AUDIT-01 · P2 · 触摸取消仍然发射

- 复现：手机模拟环境，在弹盘有效区域按下，随后通过 Chrome CDP 发送 `touchCancel`，不发送正常的 `touchEnd`。
- 预期：取消此次瞄准，保持“等待发射”。
- 实际：立即进入“炼成进行中”，生成弹珠，随后累计真实碰撞伤害。
- 影响：系统取消手势时可能意外消耗一回合，并触发敌人反击。这里验证的是浏览器接收到取消事件后的行为，未使用真实手机触发系统手势。
- 根因：Phaser 将 `TOUCH_CANCEL` 和 `TOUCH_END` 都传入 `processUpEvents`；`src/scene.ts:59` 未区分取消事件，对任何 `pointerup` 都调用 `launch()`。
- 修复方向：跟踪当前手势的指针标识，识别原生取消事件并清除按压状态，只允许正常结束的对应手势发射。

### AUDIT-02 · P2 · 关闭说明后，空格重新打开说明

- 复现：点击“玩法说明” → 点击“明白了，继续实验” → 按空格。
- 预期：根据界面提示发射弹珠。
- 实际：说明弹窗重新打开，游戏再次暂停。方向键也被按钮焦点分支跳过。
- 影响：正常查看说明后的键盘流程中断，必须另行聚焦弹盘才能操作。
- 根因：原生 dialog 关闭后焦点返回说明按钮；`src/main.ts:115` 排除所有按钮目标，空格交给浏览器触发按钮默认行为。
- 修复方向：从游戏弹窗继续时将焦点明确还给弹盘，保留菜单按钮正常的键盘可访问性。
- 原测试漏检原因：`tests/browser.spec.ts:73` 主动调用 `#game.focus()`，绕过了真实的焦点返回路径。

### AUDIT-03 · P2 · 横屏弹盘超出可视区域

- 复现：820×600 触屏视口，点击“发射弹珠”，等待自动滚动结束。
- 实际测量：canvas 高约 771.7px，顶部 y = −116.5px，底部 y = 655.2px；视口高仅 600px，底部操作栏从 y = 525px 开始遮挡。发射器与回收区不能同时看到。
- 影响：玩家无法完整观察轨迹及回收过程，自动滚动无法解决弹盘本身过高的问题。
- 根因：`src/style.css:4` 在宽度 ≤820px 使用单列布局，canvas 宽高只按宽度推导；`src/main.ts:61` 仅居中滚动，没有约束弹盘可用高度。
- 修复方向：结合可用视口高度、横竖屏及底部操作栏确定弹盘尺寸，保证整个弹盘可见。
- 证据：[横屏游戏截图](artifacts/audit/flight-820x600.png)。
- 额外兼容性观察：320×568 下页面宽 340px，存在 20px 横向溢出，来自 `body` 的 `min-width:340px`；这是同一响应式适配范围内的额外问题。

### AUDIT-04 · P3 · 鼠标右键误发射

- 复现：在等待发射状态，对弹盘有效区域单击鼠标右键。
- 预期：不提交一次射击。
- 实际：进入“炼成进行中”，开始累计伤害。
- 影响：右键误操作会实际消耗回合。
- 根因：`src/scene.ts:52` 的按下处理没有限定鼠标主键，`pointerup` 同样不区分按键。
- 修复方向：鼠标只接收主键，触屏保留正常手势入口。

## 本轮实际执行结果

| 检查 | 结果 |
| --- | --- |
| `npm run build` | 通过；仍有 Phaser 大包体积提示 |
| `npm test` | 原有 10 / 10 通过 |
| `npm run test:e2e` | 原有 8 / 8 通过，约 1.1 分钟 |
| 新增四项缺陷回归 | 4 / 4 失败，分别复现上述四项缺陷 |
| 页面运行异常 | 探索脚本未记录到 pageerror |
| 弹盘外松手 | 验证未发射 |
| 375×667 发射后的自动滚动 | 弹盘可完整显示，canvas y = 83.5～521.4，操作栏 y = 592 |
| 八组视口 | 测量 1440×900、1024×768、821×600、820×600、390×844、375×667、320×568、844×390 |

## 复现命令和证据

保持生产预览服务运行：

```sh
npm run preview -- --port 4173 --strictPort
```

在另一个终端运行：

```sh
# 当前版本预期报告四项失败；这是用于证明缺陷的回归测试。
npx playwright test --config artifacts/audit/playwright.audit.config.ts

# 补充探索脚本
node artifacts/audit/exploratory.mjs
node artifacts/audit/input-boundaries.mjs
```

- [四项回归源码](artifacts/audit/regressions.spec.ts)
- [键鼠与八组视口记录](artifacts/audit/exploratory.json)
- [触摸取消、飞行布局、后台尝试记录](artifacts/audit/input-boundaries.json)
- 失败截图位于 `artifacts/audit/test-results/`。

## 未能下结论的范围

本轮再次尝试有界面的 Chrome 切页，但自动化窗口报告 `document.hidden=false`，没有产生真实后台事件，因此不能将“没有自动暂停”判定为游戏缺陷，也不能宣布后台暂停验证通过。真实 iOS/Android、Safari/Firefox、实际听音仍未验收。

本轮未复现崩溃、重复结算、递归分裂或重开残留；这表示相关已执行用例通过，不代表穷举所有时序。
