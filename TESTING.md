# 验证记录

日期：2026-09-06。环境：macOS、Node 22.22.3、npm 11.18.0、本机 Google Chrome。自动化通过 Playwright 和 Vitest 执行。

## 修复后结果

| 验证层 | 结果 | 范围 |
| --- | --- | --- |
| 格式 | 通过 | `npm run format:check` |
| 依赖审计 | 0 个已知漏洞 | `npm audit --audit-level=high`；Vite 7.3.6、Vitest 3.2.7、esbuild 0.28.2 |
| TypeScript + Vite 生产构建 | 通过 | `npm run build`，静态产物位于 `dist/` |
| 纯规则测试 | 10 / 10 通过 | `tests/game.test.ts` |
| 浏览器交互测试 | 16 / 16 通过 | `tests/browser.spec.ts` 与 `tests/regressions.spec.ts` |
| 正常生产版实玩录屏 | 五关胜利 | 本机 Chrome；5 次发射；58.686 秒达成胜利；64.04 秒完整录屏；累计 342 伤害；最终 5 / 5 生命 |
| 运行时网络 | 无外部请求 | 录屏清单包含请求、响应状态与响应体哈希 |
| 桌面 / 移动视觉 | 通过已列视口 | 1440×900、390×844、320×568、820×600、844×390、568×320 |

### 浏览器测试覆盖

1. 鼠标瞄准、真实 Matter 碰撞、实时累计伤害、全部弹珠回收后统一结算及重复结算保护。
2. 飞行和待结算期间暂停，验证位置、弹珠年龄、场景时间和定时器冻结；恢复后继续。
3. 飞行和待结算期间重新开始，验证旧弹珠、分裂队列、粒子、计时器和伤害不会残留。
4. 强化、火焰、闪电、分裂与暴击的实际伤害/刚体行为；闪电和分裂不递归。
5. 五关升级卡、唯一能力去重、治疗上限、胜利、失败和重新游玩。
6. 触屏点击及拖动松手发射、移动升级卡片和音效偏好。
7. 停滞轻推、按活跃墙钟执行的 16 秒超时回收、低帧率下的准时结算及已累计伤害保留。
8. `touchcancel` 不发射且不会污染下一次触摸；鼠标右键不发射；说明弹窗关闭后空格与方向键恢复。
9. 320×568、820×600、844×390、568×320 下无横向溢出，发射器、回收区与按钮均在视口内。

## 受控测试与真实游玩

边界测试显式使用开发测试桥来设定敌人/玩家生命、预装升级、固定暴击值、手动命中、提前回收或构造停滞。测试桥仅在 `DEV` 且 `VITE_TEST_MODE=true` 时存在，生产构建没有 `window.__alchemyTest`。这些用例用于验证边界，不作为自然通关证明。

`scripts/record-gameplay.mjs` 默认拒绝脏工作树，并记录源码提交、锁文件、构建目录、录制脚本、浏览器、请求、响应和原始视频校验值。它只进行鼠标移动、点击、暂停/继续和随机卡片中的选择，不读取或修改测试桥。只有真实胜利、无页面异常、无控制台错误和无请求失败才返回成功。

`record:package` 在生成派生媒体前校验录屏结果、原片 SHA-256 和入口资源响应；`record:export` 脱敏本机路径，导出公开报告、原始 WebM、完整 MP4、manifest 与 `SHA256SUMS`。两步均先写临时目录，完成校验后再替换目标文件。

README 动图是从完整录屏声明的两段中截取并以 1.67 倍速生成，画面明确标注 `HIGHLIGHTS · 1.67x · 2 CUTS`。Release MP4 只转码并加标签，不剪切或改变速度；原始 WebM 不做编辑。

## 历史缺陷

第一次独立验收确认了触摸取消误发射、说明弹窗后的键盘焦点、短横屏裁切和右键误发射四项问题；首次 Linux CI 又确认低帧率会把回收和结算计时拉长。原始失败结果保存于 `evaluation/baseline/`，没有改写成修复后的结果。当前实现与回归状态见 [AUDIT.md](AUDIT.md)。

## 未验证范围

- 未在真实 iOS / Android 设备及 Safari / Firefox 上人工游玩。
- 音效开关和本地偏好已自动验证，未进行扬声器真人听音评估；录屏没有音轨。
- 后台自动暂停已实现，但自动化窗口未产生可靠的真实 `document.hidden=true`，因此没有宣布该项验收通过。
- 未进行长时间性能压力测试或穷举全部物理路径与随机升级组合。

## 复现

```sh
npm ci
npm run format:check
npm audit --audit-level=high
npm test
npm run build
npm run test:e2e
```

没有 Chrome 时可先执行 `npx playwright install chromium`，再运行 `PLAYWRIGHT_CHANNEL=chromium npm run test:e2e`。
