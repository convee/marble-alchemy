# 验证记录

日期：2026-09-05。环境：macOS、Node 22.22.3、npm 11.18.0，本机 Google Chrome，通过 Playwright 执行自动化。生产预览也已在普通 Chrome 标签页打开。

## 已完成

| 验证层 | 结果 | 证据 / 范围 |
| --- | --- | --- |
| TypeScript + Vite 生产构建 | 通过 | `npm run build`，生成 `dist/`；JS gzip 约 345kB |
| 纯规则测试 | 10 / 10 通过 | `tests/game.test.ts` |
| 浏览器交互测试 | 8 / 8 通过 | `tests/browser.spec.ts`，最终一轮约 1.1 分钟 |
| 不修改状态的生产版实玩 | 五关胜利，5 次发射，剩余 5 生命 | `artifacts/natural-run.json`、`artifacts/natural-run-result.png` |
| 生产资源与错误检查 | 3 个本站请求，0 个页面异常 | `artifacts/production-smoke.json`，无外部网络请求，测试桥接为 undefined |
| 桌面视觉 | 已查看 1440×900 截图 | `artifacts/desktop-initial.png`；无横向溢出，页面高度 862px |
| 手机视觉 / 交互模拟 | 已查看 390×844、DPR 2 截图；无横向溢出 | `artifacts/mobile-initial.png`、`artifacts/mobile-upgrades.png`；Chrome 移动设备模拟，不是实机 |

### 八项浏览器测试实际覆盖

1. 鼠标瞄准点击、真实 Matter 碰撞、伤害实时累积、落底前敌人不扣血、统一结算、拒绝再次结算、重新开始清理。
2. 飞行中暂停：弹珠坐标、年龄、场景时间和整份状态在等待期间完全不变；继续后恢复。结算等待期间暂停同样冻结。飞行及待结算期间重开都不会收到旧伤害。
3. 配方组合生效：真实生成两颗分裂刚体；再次碰撞不新增分裂；强化、火焰、必定命中的测试暴击与两次闪电加成精确符合预期；超时自动回收。
4. 五次升级界面、三张不同卡片、已拥有唯一被动不再出现、治疗上限、最终胜利界面、胜利后重开恢复默认状态。
5. 玩法说明、Esc 关闭并继续、左右键与空格、音效开关持久化、生命归零失败及重新游玩。
6. 移动设备触屏点击发射、升级卡片不横向溢出、点选后进入下一关。
7. 静止弹珠触发约 850ms 的轻推，恢复重力后继续真实物理运动并完成回收。
8. 触屏按下与拖动期间不发射；松手才生成一颗弹珠，随后出现真实碰撞。

### 测试辅助与正常游玩严格区分

边界测试使用显式开启的开发测试桥接：部分用例会设定敌人/玩家生命、预装升级、固定暴击随机值、手动触发指定钉子命中、提前回收或把弹珠年龄调到 16 秒阈值附近。静止轻推测试暂时关闭一颗弹珠的重力以稳定构造停滞，再恢复重力。这些用例用于验证难以稳定复现的边界，**不能作为未经修改的游戏通关证明**。

独立的 `scripts/playthrough.mjs` 对生产地址执行过一局完整正常游玩：仅点击发射和选卡，不访问测试桥、不改生命、不改伤害、不固定抽卡随机值。本次逐关数据如下：

| 关卡 | 本关发射伤害 | 碰撞数 | 选取升级 | 剩余生命 |
| --- | --- | --- | --- | --- |
| 1 | 17 | 17 | 分裂 | 5 |
| 2 | 28 | 28 | 火焰 | 5 |
| 3 | 56 | 28 | 闪电 | 5 |
| 4 | 112 | 28 | 强化 | 5 |
| 5 | 140 | 28 | 强化 | 5 |

最终状态为“炼金完成”，页面异常 0。随机配方和玩家瞄准会影响其他局的结果；该记录仅证明本局可以正常通关，不代表所有策略必胜。

## 未验证 / 限制

- 未在真实 iOS / Android 设备、Safari、Firefox 上实测，也未进行低端手机性能测试。
- 已验证音效开关与本地偏好；未进行真人听音验收或扬声器输出测量。
- 后台自动暂停已实现 `visibilitychange` / `pagehide` 处理，但本次自动化环境的标签页切换没有产生真实 hidden 状态，因此不能声称完成了真实切后台验收。手动暂停及暂停计时边界已通过。
- 没有长时间压力测试、全部随机升级组合的统计平衡分析，也未穷举所有物理路径。
- 没有部署公网；当前交付为本地可运行源码和静态构建。完整 Phaser 运行时导致 Vite 大包提示，构建未失败。

## 修复并回归的问题

- 初版钉子碰撞半径过小，平均碰撞偏少。按多方向 Matter 数值模拟调整为 10，并在实际浏览器重新游玩。
- Phaser ScenePlugin 的排队暂停产生一帧状态延后；改为 Scene Systems 同步暂停并通过精确快照冻结测试。
- 关闭升级弹窗后旧卡片仍留在 DOM；现在关闭即清空内容，测试已验证下一关不会读取旧卡片。
- 小屏发射时自动将弹盘滚入视野，弹盘底部补充实时伤害和碰撞计数。1440×900 的桌面布局也已压缩到可直接操作。
